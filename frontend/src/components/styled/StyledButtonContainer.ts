import styled from "@emotion/styled";

export const StyledButtonContainer = styled.div`
  display: flex;
  align-items: flex-start;
  justify-content: center;
  position: fixed;
  bottom: 0;
  width: 100%;
  height: 5rem;
  gap: 0.25rem;
  z-index: 6;
  background: linear-gradient(transparent, var(--color-black) 50%);
`;
